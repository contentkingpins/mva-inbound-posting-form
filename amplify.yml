version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Setting up build environment"
        - nvm use 16
        - echo "Making config injection script executable"
        - chmod +x scripts/inject-config.js
    build:
      commands:
        - echo "🔧 Starting permanent configuration solution build..."
        - echo "Using build-time config injection instead of external config.json files"
        
        # Run the configuration injection script
        - echo "Running build-time configuration injection..."
        - node scripts/inject-config.js
        
        - echo "Building application with injected configuration..."
        - mkdir -p dist
        - mkdir -p dist/js
        - mkdir -p dist/dashboard
        - mkdir -p dist/dashboard/js
        
        # Copy the AppConfig module to both locations for proper path resolution
        - echo "Copying AppConfig module to correct locations..."
        - cp js/app-config.js dist/js/ 2>/dev/null || echo "app-config.js not found in js/"
        - cp js/app-config.js dist/dashboard/js/ 2>/dev/null || echo "Creating dashboard js directory"
        
        # Copy all dashboard files
        - cp -R dashboard/* dist/dashboard/
        
        # Copy root files
        - cp *.html dist/ 2>/dev/null || echo "No root HTML files to copy"
        - cp *.css dist/ 2>/dev/null || echo "No root CSS files to copy"
        - cp *.js dist/ 2>/dev/null || echo "No root JS files to copy"
        
        # Copy entire js directory to maintain structure
        - cp -R js/* dist/js/ 2>/dev/null || echo "JS directory copied"
        
        # Also copy critical files to root for backward compatibility
        - cp dashboard/app.js dist/ 2>/dev/null || echo "app.js copied"
        - cp dashboard/critical-path.js dist/ 2>/dev/null || echo "critical-path.js copied"
        
        # Copy version file if it exists
        - cp version.js dist/ 2>/dev/null || echo "No version.js file to copy"
        
        - echo "✅ Build completed with permanent configuration solution!"
        - echo "🚀 Zero external config dependencies - faster, more reliable startup!"
        
        # Verify build output
        - echo "Verifying build output structure..."
        - ls -la dist/ || true
        - ls -la dist/js/ || true
        - ls -la dist/dashboard/ || true
        - ls -la dist/dashboard/js/ || true
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
  customHeaders:
    # HTML files - no cache for content files
    - pattern: '**/*.html'
      headers:
        - key: 'Cache-Control'
          value: 'no-cache, no-store, must-revalidate'
        - key: 'Pragma'
          value: 'no-cache'
    
    # JavaScript files - long cache (they have injected config now)
    - pattern: '**/*.js'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
    
    # CSS files - long cache
    - pattern: '**/*.css'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
    
    # Images - long cache
    - pattern: '**/*.{png,jpg,jpeg,gif,svg,ico}'
      headers:
        - key: 'Cache-Control'
          value: 'public, max-age=31536000, immutable'
