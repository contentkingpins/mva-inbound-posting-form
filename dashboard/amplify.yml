version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build dependencies for this static site"
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: [] 